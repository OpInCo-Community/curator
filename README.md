#Curator Backend Api

### Basic Config & tools

1. Use `pipenv` as a packaging tool.

### Postgres Configurations (Local Setup)

`server` = `localhost`
`port` = `5432`
`username` = `postgres`
`database` = `localhost`
`password` = `admin`
